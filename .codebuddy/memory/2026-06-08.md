# 2026-06-08 llm-proxy Wails v2→v3 移植决策

## 完成的拷问会

通过 grill-with-docs skill 对 llm-proxy Wails v2→v3 移植方案进行了完整决策树拷问，冻结了 10 项架构决策。

## 关键决策

| 决策 | 结论 |
|---|---|
| 移植策略 | 分支 A: 分层移植，internal/ 零修改 |
| Go 服务拆分 | 3 个 Wails 绑定服务: ProviderService/StatsService/AppService |
| 绑定层 vs 业务层 | 绑定服务 → internal/service(业务层)，保留分层 |
| 依赖注入 | main.go 手动构造 + 显式注入 |
| 生命周期 | 降级启动，CleanupService 自管理 goroutine |
| 前端路由 | react-router-dom |
| UI 组件 | Tailwind + shadcn/ui |
| 数据获取 | 自定义 hooks 封装 |
| 全局状态 | 单 React Context |
| Go 模块名 | `module github.com/wanglejiu/llm-proxy` (选项B) |

## 文档产出

- `.v3/CONTEXT.md` — 领域术语表
- `.v3/docs/adr/0003-wails-v2-to-v3-migration.md` — 迁移 ADR
