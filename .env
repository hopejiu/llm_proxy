# 数据库类型: mysql 或 sqlite
DB_TYPE=mysql

# SQLite数据库文件路径 (仅当DB_TYPE=sqlite时生效)
DB_PATH=llm_proxy.db

# MySQL配置 (仅当DB_TYPE=mysql时生效)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=wang
DB_NAME=llm_proxy

# 服务端口
WEB_PORT=80
PROXY_PORT=8888
